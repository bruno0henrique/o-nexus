Future<String> saveBytesToClientFolder({
  required List<int> bytes,
  required String directoryPath,
  required String fileName,
}) async {
  throw UnsupportedError(
    'Salvamento local de arquivos nao e suportado nesta plataforma.',
  );
}
